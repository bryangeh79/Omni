// Omni DB — Prisma client export
// Run `pnpm db:generate` once after cloning to generate schema-specific types.
export { PrismaClient } from '@prisma/client'
export type { Prisma } from '@prisma/client'
